<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:displaymode="navigation_view"
    >
    <group android:checkableBehavior="single"> <!--stay selected, one only selected at a time-->
        <item android:id="@+id/nav_home"
            android:icon="@drawable/ic_home_black_24dp"
            android:title="Home"
            app:showAsAction="never"/>
        <item android:id="@+id/nav_personal"
            android:icon="@drawable/ic_person_black_24dp"
            android:title="Personal"/>
        <item android:id="@+id/nav_shared"
            android:icon="@drawable/ic_supervisor_account_black_24dp"
            android:title="Shared"/>
        <item
            android:id="@+id/nav_chat"
            android:icon="@drawable/ic_chat_black_24dp"
            android:title="Chat" />
    </group>

    <item android:title="Control"><!-- seperate section gets created with options thaat don't stay selected -->
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings_black_24dp"
                android:title="Settings" />
            <item
                android:id="@+id/nav_about"
                android:icon="@drawable/ic_public_black_24dp"
                android:title="About"/>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_power_settings_new_black_24dp"
                android:title="Logout"
                />
        </menu>
    </item>
</menu>
